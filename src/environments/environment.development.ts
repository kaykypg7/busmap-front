/**
 * Configuração de ambiente para DESENVOLVIMENTO
 */
export const environment = {
    production: false,
    apiUrl: '/api', // Usa proxy em desenvolvimento (aponta para localhost:8080)
    backendUrl: 'http://localhost:8080' // URL do backend local
};
